<%= form_with(model: continents_population, local: true) do |form| %>
  <% if continents_population.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(continents_population.errors.count, "error") %> prohibited this continents_population from being saved:</h2>

      <ul>
      <% continents_population.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :population %>
    <%= form.text_field :population %>
  </div>

  <div class="field">
    <%= form.label :contribution %>
    <%= form.text_field :contribution %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
